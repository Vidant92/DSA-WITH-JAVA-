class Solution {
    public int elevatorRequests(int n, int[] requests){
        int currentfloor = 0;
        int time = 0;

        for(int request : requests){
            time += Math.abs(currentfloor - request);
            currentfloor = request;
        }
        return time;
    }
}
